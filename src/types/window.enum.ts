export const enum WINDOW_STATE {
  invoke = 'invoke_window',
  locker = 'locker_window',
  store = 'store_window',
  shards = 'shards_window',
  vault = 'vault_window',
  leaderboard = 'leaderboard_window',
}
